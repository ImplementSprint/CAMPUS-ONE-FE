# Campus One UI

Shared web UI components belong here once both `apps/web-lms` and `apps/web-school` use them.

Keep app-specific components inside their owning app until there is real cross-app reuse.
