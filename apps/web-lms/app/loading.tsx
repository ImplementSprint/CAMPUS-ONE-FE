import { LoadingState } from '@campus-one/ui';

export default function Loading() {
  return (
    <LoadingState
      title="Loading Campus One"
      description="Preparing the platform workspace."
    />
  );
}
