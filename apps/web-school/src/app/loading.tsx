import { LoadingState } from '@campus-one/ui';

export default function Loading() {
  return (
    <LoadingState
      title="Loading school portal"
      description="Preparing the current school workspace."
    />
  );
}
